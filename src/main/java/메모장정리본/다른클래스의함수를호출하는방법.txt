

1. 해당 함수가 인스턴스 멤버이면
    클래스명 변수명 = net 클래스명()
    변수명.함수();

2. 해당 함수가 인스턴스 멤버이면
    new 클래스명().함수();

3. 해당 함수가 정적(static) 멤버이면
    클래스명.함수();

4. 해당 클래스가 싱글톤( 프로그램내 무조건 하나의 객체만 갖는 패턴 )
    클래스명.getintance().함수();

5. @Autowrired  [ IOC , DI ]
    1. (IOC) 스프링 컨테이너(JVM만들어진 메모리 저장소) 등록 [빈(객체)] 된경우
            @Controller , @RestController , @Service , @Repository 등등
            - 반복적으로 사용할 클래스들의 객체가 주로 쓰인다 ( MVC 클래스들 )
            - DTO : X
    2. (DI) 스프링 컨테이너 (JVM만들어진 메모리 저장소) 등록 된 [빈(객체)] 호출
            @Autowrired